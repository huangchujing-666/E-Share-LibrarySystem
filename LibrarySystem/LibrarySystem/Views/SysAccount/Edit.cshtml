@{
    ViewBag.Title = "帐号编辑";
}
@using LibrarySystem.Domain;
@model LibrarySystem.Admin.Models.SysAccountVM
<div class="wrapper wrapper-content animated fadeInRight">
    <div class="row">
        <div class="col-sm-12">
            <div class="ibox float-e-margins">
                <div class="ibox-title">
                    <h5>
                        帐号编辑 <small>添加、修改</small>
                    </h5>
                </div>
                <div class="ibox-content">
                    <form id="from" class="form-horizontal">
                        <input type="hidden" class="form-control" name="id" id="id" value="@Model.SysAccount.SysAccountId">
                        <input type="hidden" id="imgInfoId" value="@Model.SysAccount.BaseImageId" />
                        <input type="hidden" id="SystemRoleId" value="@LibrarySystem.Admin.Common.Loginer.RoleId" />
                        <input type="hidden" id="oldImgInfoId" value="@Model.SysAccount.BaseImageId" />
                        <div class="form-group">
                            <label class="col-sm-2 control-label">图片</label>
                            <div class="col-sm-6">
                                <input id="filename" name="filename" type="file" class="file-loading" multiple data-show-upload="true" data-show-caption="true">
                            </div>
                        </div>
                        <div class="hr-line-dashed"></div>

                        @if (LibrarySystem.Admin.Common.Loginer.UniversityId == 0)
                        {
                            <div class="form-group">
                                <label class="col-sm-2 control-label">角色</label>
                                <div class="col-sm-2">
                                    <select @(1 == 1 ? "" : "disabled") class="form-control" name="roleId" id="roleId">
                                        <option value="0">请选择角色</option>
                                        @if (Model.SysRoles != null && Model.SysRoles.Count > 0)
                                        {
                                            foreach (var item in Model.SysRoles)
                                            {
                                                <option @(Model.SysAccount.SysRoleId == item.SysRoleId ? "selected" : "" ) value="@item.SysRoleId">@(item.Name ?? "")</option>
                                            }
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="hr-line-dashed"></div>
                        }
                        else if (LibrarySystem.Admin.Common.Loginer.RoleId == (int)EnumHelp.RoleTypeEnum.管理员)
                        {
                            <div class="form-group">
                                <label class="col-sm-2 control-label">角色</label>
                                <div class="col-sm-2">
                                    <select @(1 == 1 ? "" : "disabled") class="form-control" name="roleId" id="roleId">
                                        <option selected value="@((int)EnumHelp.RoleTypeEnum.学生)">学生</option>
                                    </select>
                                </div>
                            </div>
                            <div class="hr-line-dashed"></div>
                        }
                        else
                        {
                            <div class="form-group">
                            <label class="col-sm-2 control-label">角色</label>
                            <div class="col-sm-2">
                                <select disabled class="form-control" name="roleId" id="roleId">
                                    <option value="0">请选择角色</option>
                                    @if (Model.SysRoles != null && Model.SysRoles.Count > 0)
                                    {
                                        foreach (var item in Model.SysRoles)
                                        {
                                            if (item.SysRoleId == (int)EnumHelp.RoleTypeEnum.学生)
                                            {
                                                <option @(Model.SysAccount.SysRoleId == item.SysRoleId ? "selected" : "" ) value="@item.SysRoleId">@(item.Name ?? "")</option>
                                            }
                                        }
                                    }
                                </select>
                            </div>
                            </div>
                            <div class="hr-line-dashed"></div>
                        }
                        @if (LibrarySystem.Admin.Common.Loginer.UniversityId == 0)
                        {
                            <div class="form-group form-role">
                                <label class="col-sm-2 control-label">学校</label>
                                <div class="col-sm-4">
                                    <select class="form-control inline" style="width:150px;" name="UniversityId" id="UniversityId">
                                        <option value="0">请选择学校</option>
                                        @if (Model != null && Model.UinversityList != null && Model.UinversityList.Count > 0)
                                        {
                                            foreach (var item in Model.UinversityList)
                                            {
                                                if (Model.SysAccount.UniversityId == @item.UniversityId)
                                                {
                                        <option value="@item.UniversityId" selected="selected">@item.Name</option>
                                                }
                                                else
                                                {
                                        <option value="@item.UniversityId">@item.Name</option>
                                                }
                                            }
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="hr-line-dashed form-role"></div>
                        }
                        else if (LibrarySystem.Admin.Common.Loginer.RoleId == (int)EnumHelp.RoleTypeEnum.管理员)
                        {
                            <div class="form-group form-role">
                                <label class="col-sm-2 control-label">学校</label>
                                <div class="col-sm-4">
                                    <select class="form-control inline" style="width:150px;" disabled name="UniversityId" id="UniversityId">
                                                    <option value="@LibrarySystem.Admin.Common.Loginer.UniversityId" selected="selected">@LibrarySystem.Admin.Common.Loginer.UniversityName</option>
                                    </select>
                                </div>
                            </div>
                            <div class="hr-line-dashed form-role"></div>
                        }
                        else
                        {
                            <div class="form-group form-role">
                                <label class="col-sm-2 control-label">学校</label>
                                <div class="col-sm-4">
                                    <select class="form-control inline" style="width:150px;" disabled name="UniversityId" id="UniversityId">
                                        <option value="0">请选择学校</option>
                                        @if (Model != null && Model.UinversityList != null && Model.UinversityList.Count > 0)
                                        {
                                            foreach (var item in Model.UinversityList)
                                            {
                                                if (Model.SysAccount.UniversityId == @item.UniversityId)
                                                {
                                        <option value="@item.UniversityId" selected="selected">@item.Name</option>
                            }
                        }
                                        }
                                    </select>
                                </div>
                            </div>
                            <div class="hr-line-dashed form-role"></div>
                        }
                        <div class="form-group">
                            <label class="col-sm-2 control-label">账号</label>
                            <div class="col-sm-4">
                                <input type="text" name="account" id="account" class="form-control" placeholder="请输入账号" value="@(Model.SysAccount.Account ?? "")">
                            </div>
                        </div>
                        <div class="hr-line-dashed"></div>
                        <div class="form-group">
                            <label class="col-sm-2 control-label">昵称</label>
                            <div class="col-sm-4">
                                <input type="text" name="nickName" id="nickName" class="form-control" placeholder="请输入昵称" value="@(Model.SysAccount.NickName ?? "")">
                            </div>
                        </div>
                        <div class="hr-line-dashed"></div>
                        @if (Model.SysAccount.SysAccountId == 0)
                        {
                            <div class="form-group">
                                <label class="col-sm-2 control-label">密码</label>
                                <div class="col-sm-4">
                                    <input type="password" name="passWord" id="passWord" class="form-control" placeholder="请输入密码" value="">
                                </div>
                            </div>
                            <div class="hr-line-dashed"></div>
                        }
                        <div class="form-group">
                            <label class="col-sm-2 control-label">手机号</label>
                            <div class="col-sm-4">
                                <input type="text" name="mobilePhone" id="mobilePhone" class="form-control" placeholder="请输入手机号" value="@(Model.SysAccount.MobilePhone ?? "")">
                            </div>
                        </div>
                        <div class="hr-line-dashed"></div>
                        <div class="form-group">
                            <label class="col-sm-2 control-label">备注</label>
                            <div class="col-sm-4">
                                <input type="text" name="remarks" id="remarks" class="form-control" placeholder="请输入备注" value="@(Model.SysAccount.Remarks ?? "")">
                            </div>
                        </div>
                        <div class="hr-line-dashed"></div>
                        <div class="form-group">
                            <div class="col-sm-4 col-sm-offset-2">
                                <button id="btnSave" class="btn btn-primary" type="button">保存内容</button>
                                <a class="btn btn-white" href="javascript:history.go(-1);">取消</a>
                            </div>
                        </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
        </div>
        @section script{
            <script src="@("/Scripts/plugins/fileinput/fileinput.js"+LibrarySystem.Admin.Versions.Ver)"></script>
            <script src="@("/Scripts/plugins/fileinput/zh.js"+LibrarySystem.Admin.Versions.Ver)"></script>
            <script src="@("/Scripts/ViewJs/SysAccount/sysAccountEdit.js"+LibrarySystem.Admin.Versions.Ver)"></script>
            <script type="text/javascript">


                //商家缩略图
                var img = [];

                if ('@Model.ImgInfo.Path' != '') {
                    var str = '<img src="@(Model.ImgInfo.Source + Model.ImgInfo.Path)" class="kv-preview-thumb kv-file-content file-preview-image" />';
                    img.push(str);
                }
                $('#filename').fileinput({
                    language: 'zh',
                    allowedFileTypes: ['image'],
                    allowedFileExtensions: ['jpg', 'png', 'gif'],
                    uploadUrl: '/Uploader/ImgFile?folder=SysAccount&title=系统帐号图像',
                    showUpload: false,  //是否显示上传按钮
                    showRemove: true,   //显示移除按钮
                    maxFileSize: 2000,
                    showCaption: true,  //是否显示标题
                    maxFileCount: 1,    //表示允许同时上传的最大文件个数
                    maxFileSize: 1024,   //单位为kb，如果为0表示不限制文件大小
                    initialPreview: img,
                    dropZoneTitle: '请选择需要上传的图片(200px*200px)',
                });
                //上传成功回调事件
                $("#filename").on("fileuploaded", function (event, data, previewId, index) {
                    $('#imgInfoId').val(data.response);
                });
                //移除事件
                $("#filename").on("fileclear", function (event, data, previewId, index) {
                    $.post("/Uploader/DeleteFile", { Id: $('#imgInfoId').val() }, function (data) { });
                });

                $("#roleId").change(function () {
                    if ($(this).val() != '@((int)EnumHelp.RoleTypeEnum.系统管理员)') {
                        $(".form-role").show();
                    } else {
                        $(".form-role").hide();
                    }
                });

            </script>
        }


